#!/bin/sh
file1=$(mktemp --tmpdir i3lock-XXXXXXXXXX.png)
file2=$(mktemp --tmpdir i3lock-XXXXXXXXXX.png)

scrot -z "$file1"
convert "$file1" -scale 5% -scale 2000% "$file2"
i3lock -i ${file2}

rm ${file1} ${file2}
